package org.example;

// Interface defining the behavior of a text document
public interface TextDocument {
    void open(); // Method to open the document
    void save(); // Method to save the document
}
